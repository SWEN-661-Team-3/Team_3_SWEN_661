# Deployment must provide VITE_PUBLIC_SITE_URL. Leaving it unset here makes a
# production build fail clearly instead of silently publishing localhost URLs.
VITE_APP_ENV=production
VITE_ENABLE_MOCK_FAILURES=false
VITE_SERVICE_DELAY_MS=40
